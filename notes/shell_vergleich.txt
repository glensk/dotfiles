# tcsh:
# z jump is not working
# ? ctrl+r  (so in der art)
# ? vi mode

# bash:
# - no color for .agr .pdf ... files when hitting tab
# - !$ is not expaned when hitting tab (BUT WHEN YOU HIT ENTER)
#
# + z jump is working (not in tcsh)
# + does show folderconents correctly when there is an ^Icon file or so (not good in tcsh)
# + ctrl+r is quite nice
# + vi mode (does it exist in tcsh?)
#
#
# zsh:
# + !$ is working
# + tabs in color?
# ? z works
# + ctrl+r (gibt wowas in der art)
# ? vi mode (scheint es zugeben)

