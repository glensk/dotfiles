time machine was too slow: 
    - add the drive to spotlight Privcy tab (so the backup drive is not indexed)
    - run sudo /usr/libexec/repair_packages --repair --standard-pkgs --volume /  in the terminal to repair disk permissions
