in the end it was best to install tensorflow using anaconda, this made it work well :)

sudo easy_install pip
pip install --upgrade virtualenv 

virtualenv --system-site-packages -p python3 ~/tensorflow
