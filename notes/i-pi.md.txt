cd /Users/glensk/Dropbox/Albert/scripts
git clone https://github.com/ceriottm/i-pi-mc
cd i-pi-mc
git checkout kmc-al6xxx

to EXIT an i-pi instance create a file called EXIT in the folder where i-pi is running
in case i-pi left socket files, those woudl be in /tmp/i-pi_xxx which can be deleted once the job is finished
