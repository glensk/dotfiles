\ssh -L 8000:localhost:30000 aglen@cmmc001.bc.rzg.mpg.de   starten auf eigenem rechner

dann in den browser gehen (lokal) und auf das LRZ bookmark klicken (oder auf die adresse: https://localhost:8000

        und dann mit den garching credentials einloggen

