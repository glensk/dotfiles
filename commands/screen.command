screen
	ctrl+a+d   > {deatach}
	screen -r    (reatach)
	ctrl+a+c     newtab
	ctrl+a+n     switchtab
	ctrl+a+[     now you can use pageup pagedown



to generally (not only in screen) suspend a job

control+z

to resume: fg
