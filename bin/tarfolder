#!/bin/sh
[ "$1" = "" ] && echo 'need $1 to the the foldername' && exit
[ -e "$1.tar.gz" ] && echo $1.tar.gz does already exist && exit
#tar -zcvf "$1".tar.gz "$1"
tar --use-compress-program=lbzip2 -cvf "$1.archive.tar.bzip2" $1

